


 此模块用来存放一些其他服务通用的东西的
 在其它模块使用此模块中的东西，只需要在其他模块引入此模块的依赖就能使用此模块的功能了
 特殊：
    如果是配置类的话还需要在启动类上加个注解@ComponentScan(basePackages = {"XXX"})
    这样，在其他模块项目启动时，就会先扫描自己模块中的XXX包下有没有此配置文件，
    如果没有，就会从它的依赖中的XXX包下寻找，这样就能找到引入的模块中的配置类了，也就能在自身代码中使用了
    其中，XXX是其他模块和此模块的公共包的包路径
